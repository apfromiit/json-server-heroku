{
  "name": "json-server-heroku",
  "version": "1.0.0",
  "description": "Simple json server to deploy to heroku",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "keywords": [
    "json-server, heroku, node, REST API"
  ],
  "dependencies": {
    "json-server": "^0.16.2"
  }
}
